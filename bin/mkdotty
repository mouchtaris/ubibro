#!/usr/bin/env bash

mydir="$( cd "$( dirname "$0" )" && pwd )"
rootdir="$mydir"/..
bindir="$rootdir"/bin

dotopts="$rootdir"/_dotopts
srclist="$rootdir"/_srclist

"$bindir"/mksrclist >/dev/null
"$bindir"/mkdotopts "$(< "$srclist")"
